
## catch.c
usage:
1. set FLAG_PATH, PROGRAM and USE_FAKE_FLAG in catch.c
2. make , 64 or 32
3. use output binary file to replace the real vuln file

## traffic2exp.py
usage:

python traffic2exp.py [traffic_file_path], traffic_file is generated by catch.c's binary file

## generate_danger_out_put.py
usage:
python generate_danger_out_put.py [dagerous_output]

generate base64, base32, ... encode of param, use output to replace danger_outputs and danger_outputs_count in catch.c to detect the output